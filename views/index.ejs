<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Jokes</title>
</head>
<body>

    <div class="px-4 pt-5 my-5 text-center border-bottom">
        <h1 class="display-4 fw-bold text-body-emphasis">Click on a button to get a relevant joke!</h1>
        <div class="col-lg-6 mx-auto">
            
            <% if (locals.joke || (locals.setup && locals.delivery)) { %>
                <%  if(type.type == "single") { %>
                    <p class="lead mt-4 mb-4"><%= joke %></p>
                <%  } else { %>
                    <h4 class="lead mt-4 mb-4"><%= setup %></h4>
                    <p class="lead mb-4"><%= delivery %></p>
                <%  } %>
            
            <% } else {  %>
                <p class="lead mt-4 mb-4">No joke to display, press any of the buttons!</p>
            <%  } %>
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mb-5">
            <form action="/handle-joke" method="POST">
            <button type="submit" name="jokeType" value="Programming" class="btn btn-outline-secondary btn-lg px-4">Programming</button>
            <button type="submit" name="jokeType" value="Christmas" class="btn btn-outline-secondary btn-lg px-4">Christmas</button>
            <button type="submit" name="jokeType" value="Spooky" class="btn btn-outline-secondary btn-lg px-4">Spooky</button>
            <button type="submit" name="jokeType" value="Pun" class="btn btn-outline-secondary btn-lg px-4">Pun</button>
            </form>
          </div>
        </div>
        <div class="overflow-hidden" style="max-height: 30vh;">
          <div class="container px-5">
            <img src="/assets/hero.png" class="img-fluid border rounded-3 shadow-lg mb-4" alt="Example image" width="700" height="500" loading="lazy">
          </div>
        </div>
    </div>

    <p class="text-center">Made by Johan Bantjes</p>
    
</body>
</html>